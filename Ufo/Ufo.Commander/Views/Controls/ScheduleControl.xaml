<UserControl x:Class="Ufo.Commander.Views.Controls.ScheduleControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:Ufo.Commander.Resources"
        xmlns:local="clr-namespace:Ufo.Commander.Views"
        xmlns:ctrl="clr-namespace:Ufo.Commander.Views.Controls.Calendar"
        mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>

        <Calendar Grid.Column="0" />

        <Grid Grid.Column="1">
            <ctrl:CalendarDayControl x:Name="customCalendar" DataContext="{Binding DaySchedule}"/>
        </Grid>
    </Grid>
</UserControl>
