<UserControl x:Class="Ufo.Commander.Views.Controls.CategoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resx="clr-namespace:Ufo.Commander.Resources"
             xmlns:local="clr-namespace:Ufo.Commander.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical" Margin="30, 50">
        <GroupBox Margin="5" Header="{x:Static resx:Resources.CategoryText}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0"
               Name="lblShortName" 
               Margin="4"
               Content="{x:Static resx:Resources.IdentifierText}"/>

                <TextBox Grid.Row="0" Grid.Column="1"
                Name="txtIdentifier" 
                Margin="5"
                Text="{Binding Identifier}"
                FontSize="14"/>

                <Label Grid.Row="1" Grid.Column="0"
                   Name="lblName" 
                   Margin="4"
                   Content="{x:Static resx:Resources.CategoryText}"/>

                <TextBox Grid.Row="1" Grid.Column="1" 
                Name="txtName" 
                Margin="5"
                Text="{Binding Name}"
                FontSize="14"/>
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>
